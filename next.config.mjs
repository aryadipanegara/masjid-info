/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["blogger.googleusercontent.com"], // Tambahkan domain gambar eksternal di sini
  },
};

export default nextConfig;
